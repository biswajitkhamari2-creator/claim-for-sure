{
  "name": "claimforsure-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && node -e \"import('node:fs').then(fs => { if(fs.existsSync('dist')) fs.cpSync('dist','../dist',{recursive:true}); });\"",
    "vercel-build": "cd frontend && npm install && npm run build && node -e \"import('node:fs').then(fs => { if(fs.existsSync('dist')) fs.cpSync('dist','../dist',{recursive:true}); });\""
  }
}
